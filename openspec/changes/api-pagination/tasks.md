# Tasks: API Pagination

**TDD convention**: every `RED→GREEN` task means writing the failing tests named exactly as the scenario they come from, confirming they fail, then implementing the minimum that makes them pass (`strict_tdd: true`).

- [x] 1 RED→GREEN `pagination/pagination.spec.ts`: `resolvePage` returns page 1, limit 20 and skip 0 with no values; keeps a given page and limit; computes skip as `(page - 1) * limit`. Implement `pagination/pagination.ts` (`DEFAULT_PAGE`, `DEFAULT_LIMIT`, `MAX_LIMIT`, `resolvePage`) and `pagination/dto/pagination-query.dto.ts` (`page` and `limit`, optional whole numbers, `@Min(1)`, `limit` `@Max(MAX_LIMIT)`, documented with JSDoc and `@example`). Defaults live only in `resolvePage`, not also as property defaults, so they cannot diverge.
- [x] 2 RED→GREEN `ticket-categories.e2e-spec.ts`: "A list returns the first page by default"; "A client can ask for a specific page and size"; "A page past the end is empty"; "An invalid page or size is rejected"; "Paging through a list yields every record exactly once". Implement `pagination/dto/paginated.dto.ts`, `categories/dto/paginated-categories.dto.ts`, and page `CategoriesService.findAll` (`skip`/`limit` plus `countDocuments` with the same filter, keeping the collation sort) and its controller signature. Update the file's `listCategories` helper to read `items`, so the 10 existing scenarios keep passing unchanged. Two tests outside that helper also read the list body and had to follow: the requester check in `user-access.e2e-spec.ts` and the restart check.
- [x] 3 Verify the contract documents itself: `GET /categories` in `/docs-json` shows `PaginatedCategoriesDto` with `items`, `total`, `page` and `limit`, the query parameters carry descriptions and examples, and 400 is declared. Fix with explicit decorators only if the plugin does not infer the inherited properties. The plugin does walk the base class, so no decorator was needed. Finding: the plugin lives in `nest-cli.json`, so it runs in the Nest compiler and never under Vitest; inferred metadata can only be verified against a built app. Swagger setup moved to `src/openapi.ts` so a test can read the document, and `test/openapi.e2e-spec.ts` asserts what the decorators declare: every operation except login requires the bearer scheme, and the route list is the expected one.
- [ ] 4 `AGENTS.md`: add the convention "lists that can grow are paginated with `PaginationQueryDto` and answer with a `PaginatedDto`; fixed reference lists, such as `GET /users`, are not". Note the `?limit=100` call the category dropdown will use.
- [ ] 5 Record the debt for `DECISIONS.md` (change `ticket-system-v1`, task 9.2): deep offsets degrade because `skip` walks the skipped documents, and the migration is a cursor on the sort key.
