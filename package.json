{
  "private": true,
  "scripts": {
    "prepare": "husky",
    "dev": "docker compose up -d mongo && pnpm -r --parallel dev",
    "check": "pnpm run format:check && pnpm -r lint && pnpm -r test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.9.7"
  }
}
